[pytest]
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
markers =
    db: requires a seeded primary and shadow database
    live: calls a real model provider; costs tokens and is nondeterministic
