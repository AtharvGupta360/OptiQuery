# OptiQuery runtime + dev dependencies.
# Pinned loosely on minor versions so a clean clone resolves reproducibly.
psycopg[binary]==3.2.9
anthropic==0.69.0
fastapi==0.115.12
uvicorn[standard]==0.34.2
jinja2==3.1.6
pytest==8.3.5
